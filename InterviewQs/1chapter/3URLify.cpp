#include <iostream>
#include <vector>

using namespace std;

int main() {
string s;
cin >> s;
int last = prev = 0;
for(int i=0; i < s.size(); ++i) {
	if(s[i] == ' ')
		s.insert(k-1,"%20");
}
cout << s << endl;
return 0;
}
